import 'package:flutter/material.dart';

class ReadNotifications extends StatelessWidget {
  const ReadNotifications({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Read Notifications')),
    );
  }
}
