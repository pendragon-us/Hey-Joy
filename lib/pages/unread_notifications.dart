import 'package:flutter/material.dart';

class UnreadNotifications extends StatelessWidget {
  const UnreadNotifications({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('unread Notifications')),
    );
  }
}
